OPENAI_API_KEY=your_api_key_here

# Set to "true" to enable AI concept-visual generation in the intake flow.
# Concept visuals are exploratory references for discussion only — never the
# final design and never part of the maker-facing brief as a build spec.
ENABLE_CONCEPT_VISUALS=false
